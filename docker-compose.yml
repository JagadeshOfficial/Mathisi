version: '3.8'

services:
  mathisi-app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: mathisi-app
    restart: always
    ports:
      - "80:3000"
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - HOSTNAME=0.0.0.0
